# tilemap-editor-vue

Vue 3 + Vite 的 2D 瓦片地图编辑器（Windows 11 浅色风格 UI）。

## 依赖

- Node.js 18+ 与 npm
- 安装：`npm install`

## 命令

| 命令 | 说明 |
|------|------|
| `npm run dev` 或 `python dev.py` | 开发服务器 |
| `npm run build` 或 `python build.py` | 生产构建 → `dist/` |
| `npm run preview` 或 `python run.py` | 预览 `dist/` |
| `python test.py` | 冒烟（执行 build） |
| `python publish.py` | 构建并提示 `dist/` 路径 |

## 地图 JSON

见仓库内 `ai-software-engineering/02-physical/tilemap-editor-vue/spec.md`。
